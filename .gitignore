# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# GRPO checkpoint output dirs (very large — ~7-12 GB each; artifacts are on HF Hub)
driftdesk/driftdesk_grpo_output/
driftdesk/driftdesk_grpo_output_archived_batch0/

# Trained adapters & SFT warmup (large safetensors — available on HF Hub)
driftdesk_adapter/
driftdesk_sft_warmup/
driftdesk/driftdesk_adapter/
driftdesk/driftdesk_sft_warmup/

# Training logs (35k+ lines; raw artifact, not committed)
driftdesk/local_training.log

# Generated CSV outputs (keep baselines, ignore noisy intermediates)
driftdesk/grpo_training_results.csv
driftdesk/grpo_eval_during_training.csv

# Environment / secrets
.env
*.env

# OS
.DS_Store
Thumbs.db
